# Prometheus scrape config for LessonForge.
# The API exposes /metrics via prometheus-fastapi-instrumentator.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "lesson-forge-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
