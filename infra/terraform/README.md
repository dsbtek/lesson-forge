# infra/terraform/

Terraform for cloud infrastructure (managed Postgres with pgvector, Redis, an
S3-compatible bucket, container registry, and the compute to run api/worker/web).

Not used in Phase 1 — local development runs via `docker-compose.yml` at the repo
root. This is a later-phase deliverable.
