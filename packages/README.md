# packages/

Shared code intended to be reused across `apps/api` and `apps/web` (and future
services) — e.g. shared TypeScript types generated from the API's OpenAPI schema,
prompt templates, or a common eval harness.

Empty in Phase 1. Populated as shared concerns emerge.
