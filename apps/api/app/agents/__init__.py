"""Agent stubs.

Each module has one primary responsibility and a well-defined input/output
contract (README section 8). These implementations are **deterministic stubs** —
no LLM calls — so the graph, worker, streaming, and persistence can be exercised
end to end. Replace the bodies with real LLM/RAG logic in Phase 2/3.
"""
